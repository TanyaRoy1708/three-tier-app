apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-volume-claim-dynamic
  namespace: three-tier
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 5Gi
